<Window x:Class="SKQSwitch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SKQSwitch"
        mc:Ignorable="d" Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen" Topmost="True" Closing="Window_Closing"
        Title="激光研究所-软件切换" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel x:Name="viewModel"/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Command="{Binding PauseCommand}" Content="暂停"/>
        <TextBox Text="{Binding Info}" IsReadOnly="True" VerticalScrollBarVisibility="Auto" Grid.Row="1"/>
    </Grid>
</Window>
